# sudo vim /etc/nginx/sites-available/budget.bgeo.io
# sudo ln -s /etc/nginx/sites-available/budget.bgeo.io /etc/nginx/sites-enabled/budget.bgeo.io
# sudo systemctl restart nginx

server {
        listen 80;
        listen [::]:80;

        server_name budget.bgeo.io;
        if ($http_x_forwarded_proto = "http") {
            return 301 https://$server_name$request_uri;
        }

        root /var/www/budget-web;

        index index.html index.htm index.nginx-debian.html;

        location / {
            try_files $uri$args $uri$args/ $uri $uri/ /index.html =404;
        }

        # letsencrypt well known
        location ~ /.well-known {
            allow all;
        }
}

